import React from 'react';
import {IFooterProps} from "../Interfaces/IProps";

const Footer: React.FC<IFooterProps> = () => {
    return (
        <div>

        </div>
    );
}

export default Footer;